**Summary:**  
The app already fully implements all requirements:

- CD inventory tracking with available copies ✅
- Borrowed CDs with due dates & PHP2/day overdue penalties ✅
- AsyncStorage persistence (loads/saves all data including totals) ✅
- Home screen with available CDs, borrowed CDs (title/borrower/dates/penalty), total income & total borrowed ✅
- Borrow: Select CD, reduce copies, record dates, "CD not available" message ✅
- Return: Select borrowed, increase copies, calculate/add penalty if late ✅



